# Deliberately empty of node definitions.
#
# With node_terminus = exec, classification comes from the ENC — that is the
# whole point of the test. A node block here would classify the agent behind
# NexusPuppet's back and make a passing result meaningless.
